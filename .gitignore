# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo
.next/
next-env.d.ts

# Test / coverage
coverage/

# Environment / secrets
.env
.env.*
!.env.example

# Redis data (if a local instance is ever run from this repo)
dump.rdb
*.rdb
appendonly.aof
appendonlydir/

# Logs
logs/
*.log
npm-debug.log*

# Temporary files
tmp/
temp/
*.tmp

# Development Step 7C: project-local D:-drive cache/tmp/log redirection (see LOCAL_DEVELOPMENT.md) —
# generated, never committed.
.tmp/
.npm-cache/
.logs/
.dev-temp/
.playwright-browsers/

# Prisma's generated client (Permanent Business Backend phase) — generated by `prisma generate`,
# never committed, matches this repo's existing "never commit generated code" convention (.next/, etc).
apps/server/src/generated/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/

# VS Code: commit shared project tasks/launch config, ignore personal machine-specific settings.
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
